Interesting — the working tree is already clean. The `CLAUDE.md` appears to have been reverted or already committed. 

**Current state:**
- **anc_sti_screening** is your priority (PROMISE VoI calibration/analysis)
- **stisim** PrEP vignettes are feature-complete on `feat/431-prep-vignettes` branch
- **syph_dx_zim** manuscript calibration is running elsewhere

**What would you like to work on?**

1. **anc_sti_screening calibration** — begin/resume posterior sampling or FetalHealth mechanism refinement
2. **anc_sti_screening VOI** — run or debug the VoI pipeline (if calibration is ready)
3. **stisim PrEP vignettes** — write documentation/examples and prepare for merge to main
4. **Check on running jobs** — monitor syph_dx_zim or other experiments on VMs
5. Something else
